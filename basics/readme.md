 # First docker container


To build the containter

docker run hello-world


To check the images

docker images

To check the running processes

docker ps -a
docker ps

To rm the container

docker rm containerID

To rm the images

docker rmi containerID


